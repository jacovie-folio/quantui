import { createTheme } from '@mui/material/styles';
import type {} from '@mui/material/themeCssVarsAugmentation';

export const DarkQuiTheme = createTheme({ palette: { mode: 'dark' } });
export const LightQuiTheme = createTheme({ palette: { mode: 'light' } });
